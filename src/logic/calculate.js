const calculate = () => {
  // calculator, btnName
  // let { total, next, operation } = calculator;

  // if (btnName === '+/-') {
  //   total *= -1;
  //   next *= -1;
  // }
};

export default calculate;
